// El operador ternario en JS es una forma corta y legante de una estructura if... else

    // Se lllama ternario porque tiene tres operadores, observemos la sintaxis
    // Condicion ? valorSiVerdadero : valorSiFalso


    // Ejemplo 1

    let age = 17;
    let message = age >= 18 ? 'Eres mayor de edad ' : 'eres menor de edad'

    console.log(message);
    
